```c
#include <stdlib.h>

int putenv(char *string);
```
환경 변수를 설정한다.
`환경 변수명=값`의 형태로 저장하면 된다.
기존에 환경 변수값이 존재한다면 변경하고,
존재하지 않는다면 `malloc()` 형태로 메모리를 할당해 추가한다.
수행에 성공하면 0을 리턴한다.

해당 프로그램 안에서 변경한 환경 변수의 값이 부모 프로세스인 "셸"에서도 유효한 것은 아니다.
